# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
.DS_Store

# Testing
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/
# pytest tmp_path leftovers (pytest-cache-files-XXXX) and local test workspaces
pytest-cache-files-*/
test-ws-*/

# Logs
logs/
*.log
output/logs/

# Output files
# NOTE: `output/*` (not `output/`) so `!output/.gitkeep` can re-include —
# a fully-ignored directory makes negation patterns ineffective in git.
output/*
!output/.gitkeep

# Cache
.cache/
output/cache/
*.tmp

# Models (large files)
models/
*.bin
*.pt
*.pth
*.onnx

# OS
Thumbs.db
.DS_Store
$RECYCLE.BIN/

# Environment
.env
.env.local
.env.*.local

# Local config overrides
config.local.json
my_config.json
*.local

# Cookies (sensitive)
cookies.txt
*.cookies

# One-click Web UI launcher state
.one-click.pid
.one-click.log
web-workspace/

# Archive (development history, not tracked)
archive/

# Agent local data (WorkBuddy memory/skills/identity — local only, not version-controlled)
.workbuddy/
