# MediaScribe 项目架构全景分析

> 基于对项目全部目录逐文件梳理（mediascribe/ 核心包、douyin_batch/、docs、infra、output 样本）的完整认知。
> 当前版本：`v3.2.0e`（开发中，未提交）；已发布 `v3.1.0`。许可证 MIT。依赖 Python ≥ 3.8。

---

## 1. 项目定位

**MediaScribe** 是一个**离线、可自托管的"视频/音频/文章 → 文字"转录工具**，强调"100% 离线、不上传数据、无需 API key"。

- 输入：本地文件 / B站(BV) / 抖音 / YouTube / TikTok / 小红书 / 微信公众号 / 短链(t.cn 等)
- 处理：下载 → 抽音频(ffmpeg) → Whisper 系列 ASR 转录 → 术语校正 + 可选 LLM 后处理
- 输出：结构化 **Markdown 转录稿** + 元数据 JSON + 可选汇总报告
- 接口：CLI、FastAPI Web UI、Chrome/Edge 扩展、MCP 服务（供 AI Agent 调用）
- 形态：可 pip 安装、可 Docker 部署、可导入为 Python 库

---

## 2. 技术栈

| 类别 | 技术 | 用途 |
|---|---|---|
| 下载 | `yt-dlp` | B站/YouTube 等通用下载（必需） |
| 下载 | `requests` + `urllib3` | 抖音/小红书/公众号 HTTP 抓取 |
| 浏览器自动化 | `playwright`(Chromium) | 抖音/小红书渲染页抓真实媒体 CDN（反爬 flag） |
| ASR 引擎 | `openai-whisper`(torch) | 默认引擎 |
| ASR 引擎 | `faster-whisper`(CTranslate2) | 可选，线程安全模型缓存 + VAD |
| ASR 引擎 | `whisperx` | 可选，对齐 + 说话人分离(pyannote) |
| 音频 | `ffmpeg` / `ffprobe` | 抽 16k 单声道 WAV、长音频分块 |
| VAD | `webrtcvad` | 语音活动检测分块（可选） |
| LLM 后处理 | `openai` SDK | OpenAI 兼容 API，默认 DeepSeek |
| OCR | `paddleocr` / `pytesseract` / `easyocr` | 公众号图文 OCR（可选回退链） |
| 学习 | `pypinyin` + `difflib` | ASR 自动术语学习（拼音相似度） |
| Web/MCP | `fastapi` / `uvicorn` / `mcp` SDK | Web UI 与 MCP 服务（可选） |
| 可观测 | `opentelemetry`(可选) | 零依赖 mini-SDK 兼容实现 |
| 构建/打包 | `setuptools`、`wheel`、`docker` | 分发与部署 |
| 测试 | `pytest`、`ruff`、`mypy` | 单测/CI（`test-imports`/`test-syntax`/`test-i18n`） |
| 文档 | `mkdocs`(Material 主题) | 文档站点 22 页 |

---

## 3. 目录结构与职责总览

```
mediascribe/
├── mediascribe/          ← 核心包（38 .py）：pipeline / transcribers / downloaders / web / mcp / plugins
├── douyin_batch/        ← 抖音作者主页批量转录库（15 .py + tests/ 46 测试）
├── extension/           ← Chrome/Edge MV3 浏览器扩展（纯前端，发送 URL 给 Web UI）
├── scripts/             ← 一键启动 / benchmark / E2E 录制 等运维脚本
├── examples/            ← 插件示例（vimeo 下载器、t.cn 转换器）
├── docs/                ← 文档索引 + E2E/lint 报告
├── docs_site/           ← mkdocs 站点源（22 .md + 1 html 演示）
├── output/              ← 标准批处理产物（downloads/audio/metadata/transcripts + 汇总）
├── output-test/         ← 开发调试/人工精校工作区（含一次性脚本 + 精修 md）
├── archive/             ← 历史/废弃材料（legacy-scripts/tests、release-docs、temp-*)
├── build/               ← setuptools 构建中间产物（勿改）
├── test-ws-{auth,rl,rl-off}/ ← Web UI 测试工作区占位目录
├── config.example.json  ← 批量 CLI 的 JSON 配置样例
├── pyproject.toml / requirements*.txt / Dockerfile / docker-compose.yml / Makefile / justfile
├── README.md / HANDOVER.md / CHANGELOG.md / CONTRIBUTING.md
```

---

## 4. 核心包 `mediascribe/` 模块详解

### 4.1 顶层入口与配置
- **`__init__.py`** — 包导出入口，暴露 `Settings / Pipeline / AsyncPipeline / Transcriber / Downloader / learn` 等公共 API；`__version__ = "3.2.0a"`。
- **`__main__.py`** — CLI 入口 `main()`，分发子命令：
  - `transcribe <input>` → `Pipeline.transcribe`
  - `batch` → 批量
  - `profile <jsonl>` → 渲染性能报告
  - `learn <compare|edit|save|list|confirm|remove|export|import|clear>` → 术语学习
- **`config.py`** — `Settings` 配置类：workspace、model、engine、device、hf_token、diarization、wechat cookies、LLM 后处理；含 cookie 解析（Netscape/JSON/key=value）。
- **`models.py`** — 数据类：`SourceRef`(输入源)、`DownloadResult`、`TranscriptResult`、`SourceKind`。
- **`inputs.py`** — `parse_source()`：字符串 → `SourceRef`（识别本地文件/B站/抖音/YouTube/TikTok/小红书/公众号/短链）；`safe_stem` 委托 `douyin_batch.platform_compat`。
- **`url_utils.py`** — 短链解析、BV 提取、URL 规范化（纯标准库）。

### 4.2 Pipeline 链（核心数据流）
- **`pipeline.py`** — `Pipeline` 类：`resolve_device()`(CUDA/Metal/ROCm/CPU)、`gpu_health()`；`_get_downloader()` 智能路由；`transcribe()` 组装 `PipelineContext` 跑 stage 链；含**微信公众号分支**（`_handle_wechat_mp` / `_transcribe_video_article`）、markdown 拼接与双语标签。
- **`pipeline_stages.py`** — Stage 框架：`PipelineContext`、`PipelineCancelled`、`Stage` 基类 + 5 个具体 Stage：
  - `ParseSourceStage` → `DownloadStage` → `ExtractAudioStage` → `TranscribeStage` → `AssembleStage`
  - `default_chain()`、`_atomic_write_text`（原子写防损坏）
- **`pipeline_async.py`** — `AsyncPipeline`（基于 `asyncio.to_thread` 并发多视频）、`from_sync` 工厂、`_gpu_aware_concurrency`（v3.2.0e 显存感知）、`_FailedResult`。

### 4.3 语音/音频
- **`audio_utils.py`** — `extract_audio()` 用 ffmpeg 抽 16k 单声道 WAV（含超时）；`detect_speech_segments()`（可选 webrtcvad VAD）。
- **`transcribers/`**
  - `base.py` — `Transcriber` 抽象基类
  - `factory.py` — `get_transcriber_with_fallback`（whisperx → faster-whisper → whisper 链）
  - `whisper.py` — `WhisperTranscriber`（含 torch 2.6 兼容补丁）
  - `faster_whisper.py` — `FasterWhisperTranscriber`（模型缓存 + VAD + 简体引导）
  - `whisperx.py` — `WhisperXTranscriber`（对齐 + 说话人分离）
  - `chunked.py` — `ChunkedTranscriber` 长音频分片 + 重叠 + 合并（ffmpeg/ffprobe 切分）

### 4.4 下载器 `downloaders/`
- `base.py` — `Downloader` ABC
- `ytdlp.py` — `YtDlpDownloader`（通用）
- `douyin.py` — Playwright + requests 取真实媒体 URL
- `youtube.py` — 继承 YtDlp，player_client 调优
- `xiaohongshu.py` — Playwright 取媒体
- `wechat_mp.py` — 图文/视频/图片文章抓取 + OCR（paddleocr/pytesseract/easyocr 回退）

### 4.5 后处理 / 学习 / 缓存 / 性能 / 可观测
- **`post_process.py`** — 术语校正 `post_process_transcript`、模型推荐 `auto_select_model`、领域 prompt `get_prompt_template`、HF 镜像 `setup_hf_mirror`。
- **`learn.py`** — ASR 自动术语学习 v2：`compare` / `learn_from_edit` / `learn` / `get_learned_terms`（pypinyin 拼音相似度 + difflib）。
- **`llm_post_process.py`** — `LLMPostProcessor`（OpenAI 兼容，deepseek 默认），修正专名/同音字/标点，含重试退避。
- **`cache.py`** — `PersistentDownloadCache`(SHA-256 内容寻址)、`PersistentChunkCache`。
- **`performance.py`** — `@profile_step` 计时、`PerformanceReport`、`parallel_map`、`DownloadCache`。
- **`progress.py`** — `JobProgress` / `ProgressRegistry`（WebSocket 进度 + 取消）、`with_progress`。
- **`observability.py`** — 轻量 OpenTelemetry 兼容实现。
- **`profile_cli.py`** — `profile` 子命令，渲染 JSONL 计时为 Markdown/JSON 报告。

### 4.6 插件 / Web / MCP
- **`plugins/`** — 基于 `entry_points` 的插件系统（`mediascribe.downloaders` / `transcribers` / `url_transformers`）。
- **`web/app.py`** — FastAPI `create_app`：端点 `/api/transcribe`、`/api/jobs`、`/ws/progress/{id}`、`/api/health`、扩展下载；含 CORS、Bearer token、限流、GPU 缓存、Pipeline 缓存。
- **`web/extension_builder.py`** — 浏览器扩展 ZIP 构建。
- **`mcp_server.py`** — MCP 服务（stdio/HTTP），暴露 `transcribe_video` / `transcribe_wechat_mp` 等 8 个工具给 AI Agent。

### 4.7 模块依赖关系（有向，简版）
```
__main__ → config, pipeline → (audio_utils, downloaders, inputs, models, pipeline_stages, transcribers)
pipeline_stages → audio_utils, config, downloaders, inputs, models, transcribers, post_process, learn, llm_post_process
pipeline_async → config, pipeline, pipeline_stages, models
transcribers/factory → whisper, faster_whisper, whisperx；chunked → audio_utils, pipeline_stages
downloaders/* → config, models, base；wechat_mp → OCR(可选)
post_process → learn(懒导入)；learn → cache
web/app → config, pipeline, progress；mcp_server → config, pipeline, inputs
plugins/registry → downloaders.base, transcribers.base
外部兄弟包：douyin_batch（safe_filename / is_safe_url / ProcessCache / i18n）
```

---

## 5. `douyin_batch/` 模块详解（抖音批量转录库）

版本 `__version__ = "3.1.0"`。定位：**抖音作者主页批量转录工具库**（被文档中提到的 CLI 入口 `douyin_batch_v3.py` 调用；该入口文件当前不在本目录，推测为外部生成/独立脚本）。

| 文件 | 职责 | 关键类/函数 |
|---|---|---|
| `__init__.py` | 包元信息 | `__version__` |
| `config.py` | 配置合并(JSON/环境变量/CLI) | `BatchConfig`(dataclass) |
| `browser.py` | **Playwright 抓取**：作者主页、滚动列表、拦截真实音频 CDN | `BrowserManager`(单例)、`get_user_url_from_video`、`get_user_videos`、`get_media_url_fast` |
| `transcribe.py` | **转录桥接**：复用 Whisper | `TranscriberPool`(单例)、`transcribe_audio` |
| `cache.py` | 断点续传(JSON) | `ProcessCache` |
| `retry.py` | 通用重试 + 带重试下载 | `retry`、`download_media_with_retry` |
| `i18n.py` | 中英双语消息 | `Messages`、`t`、`set_language` |
| `agent_output.py` | AI Agent 结构化 JSON(`--json`) | `AgentOutput`、STATUS_*/STAGE_*/PLATFORM_* 常量 |
| `security.py` | URL/文件名安全校验、路径穿越防护 | `is_safe_url`、`sanitize_filename`、`safe_join_path` |
| `logger.py` | 彩色/文件日志 | `Logger`、`ColoredFormatter` |
| `platform_compat.py` | 跨平台兼容(ffmpeg 检测) | `check_ffmpeg`、`safe_filename`、`get_os` |
| `progress.py` | 进度条 + ETA | `ProgressTracker`、`format_duration` |
| `report.py` | Markdown/JSON 汇总报告 | `generate_summary_report` |

**与核心包关系：强耦合。** `transcribe.py` 直接 `from mediascribe import Pipeline, Settings`，把下载结果交给核心转录流水线；自己不实现 ASR。其余 13 个文件为叶子模块，相互不 import，由缺失的 CLI 入口统一装配。

**核心流程**：解析输入 → 用 Playwright 从单视频提取 `sec_uid` 拼主页 → 滚动抓取视频列表 → 监听 CDN 抓真实音频地址 → requests 下载(指数退避) → `mediascribe.Pipeline.transcribe(language="zh")` 生成 md → `ProcessCache` 断点续传 → `report`/`agent_output` 汇总。

---

## 6. 核心处理流程（端到端）

```
输入字符串
  → inputs.parse_source() → SourceRef
  → Pipeline._get_downloader() 智能路由
       (抖音/小红书/YouTube/公众号 用专用下载器，否则 YtDlpDownloader)
  → DownloadStage.download() → DownloadResult
  → ExtractAudioStage: audio_utils.extract_audio() → 16k WAV  (ffmpeg)
  → TranscribeStage: transcriber.transcribe()  (whisper / faster-whisper / whisperx；whisperx 含对齐+说话人分离)
  → AssembleStage: post_process_transcript() 术语校正
                    → [可选] LLMPostProcessor 后处理
                    → 写 Markdown + metadata JSON （_atomic_write_text）
  → 返回 TranscriptResult
```

异步并发：`AsyncPipeline` 用 `asyncio.to_thread` 多视频并行，v3.2.0e 起按 `MEDIASCRIBE_VRAM_PER_TASK_MB` 做 GPU 显存感知并发。

---

## 7. 配置体系

**两套配置，注意区分：**

### A. 运行期环境变量（主配置，HANDOVER §8.1）
`MEDIASCRIBE_DEVICE`(auto) / `MEDIASCRIBE_ENGINE`(whisper) / `MEDIASCRIBE_MAX_WORKERS` / `MEDIASCRIBE_VRAM_PER_TASK_MB`(3000) / `MEDIASCRIBE_RATE_LIMIT`(10) / `MEDIASCRIBE_CACHE_DIR` / `MEDIASCRIBE_WECHAT_COOKIE` / `HF_ENDPOINT` / `HF_TOKEN` 等。

### B. 批量 CLI 的 JSON（`config.example.json`，21 项）
| 键 | 默认 | 含义 |
|---|---|---|
| `headless` | true | Playwright 无头 |
| `browser_timeout` | 30 | 浏览器超时(秒) |
| `max_retries` | 3 | 重试次数 |
| `retry_delay` | 1.0 | 重试基础延迟 |
| `retry_backoff` | 2.0 | 退避倍率 |
| `download_timeout` | 30 | 下载超时(秒) |
| `max_videos` | 10 | 批量上限 |
| `max_scroll_rounds` | 10 | 主页滚动轮数 |
| `scroll_pause` | 2.0 | 滚动停顿(秒) |
| `language` | "zh" | 转录语言 |
| `whisper_model` | "small" | 模型大小 |
| `output_dir` | "output" | 输出目录 |
| `keep_audio` | false | 保留音频 |
| `workers` | 1 | 并发线程 |
| `log_level` | "INFO" | 日志级 |
| `log_to_file` | true | 写日志文件 |
| `skip_existing` | true | 跳过已存在 |
| `min_video_size_mb` | 0.01 | 最小体积过滤 |
| `max_wait_for_media` | 15 | 等待媒体最大时间(秒) |

> 注：`config.example.json` 无注释，含义由 HANDOVER/docs 推导；运行期主配置以环境变量为主。

---

## 8. 接口层（四种使用方式）

1. **CLI**：`python -m mediascribe transcribe "<URL/文件>" --language zh`；批量 `python douyin_batch_v3.py --user "<主页>" -n 20`；`profile` / `learn` 子命令。控制台入口：`mediascribe` 与 `mediascribe-batch`。
2. **Web UI**：`python -m mediascribe.web.app --port 8000`（FastAPI）。端点 `/api/transcribe`、`/api/jobs`、`/ws/progress/{id}`、`/api/health`、扩展下载 `/api/extension/download`。
3. **浏览器扩展**（`extension/`，MV3）：把当前标签页 URL 发给自托管 Web UI，展示返回的 Markdown；支持 popup + Side Panel；可从 `/extension` 一键下载 ZIP。
4. **MCP 服务**：`python -m mediascribe.mcp_server`（stdio 或 `--transport http --port 8765`），暴露 8 个工具（`transcribe_video` / `transcribe_wechat_mp` 等）给 AI Agent。Agent 友好输出：`--json`(schema `mediascribe.agent-output/v1`)、`--bilingual-json`、`--platform` 白名单。

---

## 9. 构建 / 部署 / 测试 / 文档

### 构建与分发
- `pyproject.toml`：setuptools 后端；可选 extras：`whisperx` / `faster-whisper` / `vad` / `ocr` / `mcp` / `web` / `all` / `dev` / `test`。
- 插件入口点：`mediascribe.downloaders`→vimeo、`mediascribe.url_transformers`→tcn（来自 `examples/plugins/`）。
- `Dockerfile`：多阶段 `python:3.11-slim`，非 root 用户 `mediascribe`，`EXPOSE 8000`，HEALTHCHECK 访问 `/api/health`，ENTRYPOINT 跑 Web UI。
- `docker-compose.yml`：服务 `web`，端口 `8000:8000`，挂载 `./web-workspace:/workspace`，`restart: unless-stopped`。

### 开发/自动化命令（Makefile / justfile 一致）
`install`/`dev`、`install-browser`(playwright chromium)、`test`/`test-verbose`、`test-i18n`/`test-cross`/`test-imports`/`test-syntax`、`lint`/`format`、`demo`、`clean`、`verify`、`up`/`up-daemon`/`down`(一键 Web UI，委托 `scripts/one_click_up.py`)、`docker-up`。

### scripts/
- `one_click_up.py` — 跨平台一键启动 FastAPI Web UI（daemon/端口/开浏览器/docker）。
- `benchmark_transcribers.py` — 复现 whisperx vs faster-whisper 对比（WER/RTF/显存），支持 `--engines fake` CI 路径。
- `capture_cli_help.py` / `capture_e2e_recording.py` — 快照 CLI help / 录制 E2E 供 PR 审计。
- `run_real_e2e.py` / `run_wechat_mp_e2e.py` — 真实端到端冒烟（需网络+真实 cookie）。

### 测试
- 核心包单测 + `douyin_batch/tests/`(46 测试) + `test-imports`/`test-syntax`/`test-i18n`。
- HANDOVER 记录：v3.2.0e 状态 **772 passed, 14 skipped, 0 failed**。CI 矩阵 5 Python × 3 OS = 15 cells。

### 文档站点（`docs_site/` via mkdocs，22 页）
index/getting-started、platforms、engines、plugins、wechat-mp、web-ui、one-click、browser-extension、mcp、benchmarking、chunking、performance、observability、e2e-testing、ci-matrix、roadmap-v3.2(a/b)、changelog、contributing、license，及 `ws-progress-demo.html`。

---

## 10. 典型产物（output/ 结构）

一次运行产出关联文件集：
```
output/
├── downloads/   *.mp4        源视频（下载得到，非生成）
├── audio/       *.wav        提取的中间音频（ffmpeg）
├── metadata/    *.json       处理参数（引擎/模型/语言/路径/时间）
├── transcripts/ *.md         ★ 最终交付物：基本信息表 + 纯文本转录（中文，按语义分段）
└── 作者往期内容汇总_*.md       多视频聚合汇总报告
```
> `output/` 中 122 个 `wechat_mp_*.txt` 是 64B 占位测试桩，**非真实输出**；真实转录落 `.md`。
> `output-test/` 是开发精校工作区（一次性脚本 + 带发言人标记/分段的人工精修 md，质量高于自动稿）。

---

## 11. 已知问题 / 后续路线（HANDOVER）

**已踩坑**：抖音 DASH 音视频分离(`E1`，`smart_retry_failed.py` 智能下载)；`learn.py` 多进程 read-modify-write 丢数据；TRAE Sandbox ffmpeg 限制；faster-whisper 国内 HF 模型下载超时。
**P3 backlog**：LLM 流式输出、按时间戳分段调 LLM、真实 API 联调、Web UI 后处理状态标签、CLI `--workspace` 报错。
**路线**：v3.3.0（默认引擎=benchmark 优胜者 + LLM 真实联调）、v3.4.0（分布式 pipeline、WhisperX 共识）、长期（实时流式转录、多语言 UI、插件市场）。
**接手清单**：clone → `pip install -e ".[dev]"` → `playwright install chromium` → 跑 ~751 测试 → **先与用户确认是否 commit 再开发**（v3.2.0b~e 改动尚未提交）。

---

## 12. 快速建立项目认知的建议路径

1. 先读 `HANDOVER.md`（权威架构与状态）+ `README.md`（功能/用法）。
2. 再读 `mediascribe/pipeline_stages.py` + `pipeline.py`（核心数据流）。
3. 看 `transcribers/factory.py` + 三个引擎实现（理解 ASR 抽象与回退）。
4. 看 `downloaders/` 五个实现（理解多平台抓取差异）。
5. 需要抖音批量 → `douyin_batch/browser.py` + `transcribe.py`。
6. 需要扩展/集成 → `web/app.py`(REST+WS)、`mcp_server.py`(Agent)、`extension/`(前端)、`plugins/`(入口点)。
7. 跑 `make test` / `make verify` 确认环境；用 `scripts/one_click_up.py up` 起 Web UI 实测。
