# 🎬 MediaScribe 使用说明

## ✅ 当前状态

项目已完成深度整合并测试通过！系统已准备好处理 Bilibili、YouTube 等数千个网站的视频。

---

## 📁 项目文件

| 文件 | 用途 |
|------|------|
| `test_bilibili.py` | 完整测试脚本（下载 + 转写） |
| `test_download_info.py` | 轻量级测试（仅解析源信息） |
| `mediascribe/__main__.py` | 命令行入口 |
| `mediascribe/pipeline.py` | 核心工作流 |
| `requirements.txt` | 依赖清单（已安装完成） |

---

## 🚀 快速开始

### 方式 1：使用完整测试脚本（推荐新手）

```bash
cd d:\1\mediascribe

# 运行完整测试（下载 + 转写）
python test_bilibili.py
```

### 方式 2：使用命令行（推荐）

```bash
# 基本使用
python -m mediascribe transcribe "https://b23.tv/5TmfnwG"

# 使用 small 模型（推荐，速度/质量平衡）
python -m mediascribe transcribe "https://b23.tv/5TmfnwG" --model small

# 使用更快的模型
python -m mediascribe transcribe "https://b23.tv/5TmfnwG" --model base

# 使用中文语言提升准确率
python -m mediascribe transcribe "https://b23.tv/5TmfnwG" --language zh

# 使用 WhisperX 引擎（需要额外安装）
# pip install whisperx
python -m mediascribe transcribe "https://b23.tv/5TmfnwG" --engine whisperx
```

---

## ⚙️ 模型选择

| 模型 | 大小 | 速度 | 质量 | 推荐场景 |
|------|------|------|------|---------|
| `tiny` | ~39 MB | ⚡⚡⚡ 极快 | ⭐ | 快速测试、简短视频 |
| `base` | ~74 MB | ⚡⚡ 快 | ⭐⭐ | 一般视频、快速转写 |
| `small` | ~244 MB | ⚡ 中等 | ⭐⭐⭐ | **推荐！日常使用** |
| `medium` | ~769 MB | 🐢 慢 | ⭐⭐⭐⭐ | 高要求场景 |
| `large` | ~1550 MB | 🐌 极慢 | ⭐⭐⭐⭐⭐ | 高质量要求 |

**首次运行时会自动下载模型到本地，之后无需重新下载。**

---

## 🔧 引擎选择

### 1. OpenAI Whisper（默认，已安装）

```bash
python -m mediascribe transcribe "视频链接" --engine whisper
```

### 2. WhisperX（需要先安装）

支持说话人分离和单词级别对齐，推荐安装！

```bash
# 安装 WhisperX
pip install whisperx

# 使用
python -m mediascribe transcribe "视频链接" --engine whisperx --diarization --hf-token "你的HuggingFace Token"
```

### 3. Faster Whisper（需要先安装）

whisper.cpp 的 Python 版本，速度更快！

```bash
# 安装
pip install faster-whisper

# 使用
python -m mediascribe transcribe "视频链接" --engine faster-whisper
```

---

## 📂 输出目录

```
d:\1\mediascribe\output\
├── downloads\       # 下载的视频文件
├── audio\           # 提取的音频文件
├── transcripts\     # 转写文本文件 (.txt)
└── metadata\        # 元数据文件 (.json)
```

---

## 🎯 完整示例

### 处理您提供的 Bilibili 视频

```bash
cd d:\1\mediascribe

# 方法 1: 使用测试脚本
python test_bilibili.py

# 方法 2: 直接使用命令行
python -m mediascribe transcribe "https://b23.tv/5TmfnwG" --model small --language zh
```

### 处理本地视频文件

```bash
python -m mediascribe transcribe "D:\Videos\我的视频.mp4"
```

### 处理多个视频

```bash
python -m mediascribe batch "视频1.mp4" "视频2.mp4" "https://bilibili.com/..."
```

---

## ⚠️ 注意事项

1. **FFmpeg 已就绪**：✅ 已确认 FFmpeg 在 PATH 中
2. **首次模型下载**：首次运行会自动下载 Whisper 模型，需要网络连接
3. **处理时间**：
   - tiny/base: 几分钟
   - small: ~10-20 分钟（40分钟视频）
   - medium/large: 可能需要更长时间
4. **磁盘空间**：确保有足够空间存放视频、音频和中间文件

---

## 🔍 使用小技巧

### 1. 获取 HuggingFace Token（用于 WhisperX 说话人分离）

1. 访问 https://huggingface.co/settings/tokens
2. 点击 "Create new token"
3. 创建一个类型为 "Fine-grained" 的 Token
4. 接受 https://huggingface.co/pyannote/speaker-diarization-community-1 的条款

### 2. 加快速度

- 使用 `tiny` 或 `base` 模型
- 如支持 GPU 加速，使用 CUDA

### 3. 提升准确率

- 明确指定语言（`--language zh` 表示中文）
- 使用 `small` 或更高级别模型
- 使用 WhisperX 做单词级别对齐

---

## 📞 下一步

现在您可以：

1. **立即运行**：`python test_bilibili.py` 或 `python -m mediascribe transcribe "https://b23.tv/5TmfnwG"`
2. **调整配置**：修改模型、引擎参数
3. **查看结果**：在 `output/transcripts/` 找到转写文本

祝您使用愉快！🎉
