# Dev dependencies — single source of truth is the [dev] extra in
# pyproject.toml (pytest / pytest-cov / ruff / mypy).  `-e .` also installs
# the package itself with its core runtime dependencies.
-e .[dev]
