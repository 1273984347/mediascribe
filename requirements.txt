# Runtime dependencies — single source of truth is [project.dependencies] /
# [project.optional-dependencies] in pyproject.toml.  This file is only a
# thin install shim used by `make install` / `make dev`; CI and the
# Dockerfile install extras directly via `pip install -e ".[...]"`.
#
# NOTE: the `llm` extra (openai>=1.0.0, used by mediascribe.llm_post_process)
# is intentionally NOT pulled in here — opt in with
#   pip install -e ".[llm]"      (repo checkout)
#   pip install "mediascribe[llm]"  (installed package)
-e .[web,ocr,faster-whisper,mcp]
